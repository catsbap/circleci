version: 2.1

jobs:
  build:
    docker:
      image: cimg/base:stable
    steps:
      - checkout
      - run:
          command: npm install
      - run:
          command: npm test

workflows:
  version: 2
  build-deploy:
    jobs:
      -build