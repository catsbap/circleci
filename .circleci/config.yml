version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
        auth:
          username: catsbap@gmail.com
          password: cbbarb1012%
    steps:
      - checkout
      - run:
          command: npm install
      - run:
          command: npm test